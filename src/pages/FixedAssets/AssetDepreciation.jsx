import {useEffect,useState} from "react"; import {FaCalculator} from "react-icons/fa"; import Swal from "sweetalert2"; import {Button} from "@/components/ui/button"; import {apiErrorMessage,apiJson,normalizeList} from "@/lib/api"; import DepreciationDrawer from "./DepreciationDrawer"; import NotFoundImage from "/assets/scopefinding.png";
const base=`${import.meta.env.VITE_APP_FIN_URL}/api/control/assets`; const money=v=>Number(v||0).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2});
export default function AssetDepreciation(){const[items,setItems]=useState([]),[loading,setLoading]=useState(true),[selected,setSelected]=useState();const load=()=>{setLoading(true);apiJson(`${base}?pageIndex=0&pageSize=100`).then(x=>setItems(normalizeList(x))).catch(e=>Swal.fire("Error",apiErrorMessage(e,"Unable to load depreciable assets."),"error")).finally(()=>setLoading(false))};useEffect(load,[]);return <div className="bg-white m-8 px-8 py-8 shadow-2xl rounded-lg"><div className="mb-6 flex items-center rounded-2xl bg-indigo-800 px-6 py-3"><h2 className="flex items-center gap-2 text-xl font-bold text-white"><FaCalculator/> Asset Depreciation</h2></div><div className="rounded-sm bg-gray-200 p-4"><div className="mb-4 grid grid-cols-12 gap-3 rounded-lg bg-gray-700 p-3 font-semibold text-gray-100"><span className="col-span-3">Asset</span><span className="col-span-2">Last Posted</span><span className="col-span-2 text-right">Accumulated</span><span className="col-span-2 text-right">Net Book Value</span><span className="col-span-3 text-right">Action</span></div>{loading?[1,2,3].map(i=><div key={i} className="mb-2 h-14 animate-pulse rounded bg-gray-50"/>):items.length?items.map(a=><div key={a.Id} className="mb-2 grid grid-cols-12 items-center gap-3 rounded-lg border bg-white p-4 shadow-lg hover:shadow-xl"><span className="col-span-3 font-medium text-indigo-700">{a.Name}</span><span className="col-span-2">{a.LastDepreciationDate?new Date(a.LastDepreciationDate).toLocaleDateString():"Not started"}</span><span className="col-span-2 text-right">{money(a.AccumulatedDepreciation)}</span><span className="col-span-2 text-right">{money(a.NetBookValue??a.PurchasePrice)}</span><span className="col-span-3 text-right"><Button size="sm" className="bg-amber-600 hover:bg-amber-700" onClick={()=>setSelected(a)}><FaCalculator/> Preview / Post</Button></span></div>):<div className="text-center text-gray-400"><img src={NotFoundImage} alt="No assets" className="mx-auto w-42"/><p>No depreciable assets found.</p></div>}</div><DepreciationDrawer asset={selected} baseUrl={base} onClose={()=>setSelected()} onPosted={load}/></div>}
